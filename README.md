# Context-Aware-Beginner-Friendly-and-Bug-Aware-Code-Summarization-Using-LLMs
# Context‑Aware Code Summarization with LLM

## Requirements
- Python 3.9+
- CUDA‑capable GPU (recommended, 4GB+ VRAM) or CPU (slow)
- Install dependencies: `pip install -r requirements.txt`

## Usage

1. Place your Python project in a folder (e.g., `my_project/`).
2. Run:
   ```bash
   python main.py --project_root ./my_project --target_file ./my_project/auth.py
